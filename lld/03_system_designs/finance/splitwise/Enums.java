package splitwise;

public class Enums {

    public enum SplitType {
        EQUAL,
        EXACT,
        PERCENTAGE
    }

    public enum GroupType {
        TRIP,
        HOME,
        COUPLE,
        OTHER
    }
}
